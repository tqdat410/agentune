// Mood-to-query mapping — translates mood keywords into YouTube search queries
export function getMoodQuery(_mood: string): string {
  // TODO: Phase 6 — map moods (focus, chill, hype) to curated search queries
  return '';
}
